@model Animatch.Models.Event
@{
    ViewData["Title"] = "Детайли за събитие";
}

@section HeadStyles {
    <link rel="stylesheet" href="~/css/admin-events.css" asp-append-version="true" />
}

<div class="container mt-5 event-admin-shell">
    <div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mb-4">
        <h1 class="page-title m-0">Детайли за събитие</h1>
        <a asp-area="Admin" asp-controller="Events" asp-action="Index" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-1"></i> Към събитията
        </a>
    </div>

    <article class="card h-100 event-detail-card">
        <header class="event-detail-header">
            <h3 class="event-detail-title">@Model.Title</h3>
            <span class="event-card__date">
                <i class="far fa-clock"></i>
                @Model.Date.ToString("dd.MM.yyyy HH:mm")
            </span>
        </header>

        <div class="event-meta">
            <div class="event-meta-item">
                <span class="event-meta-label">Локация</span>
                <span class="event-meta-value">
                    <i class="fas fa-location-dot me-1"></i>@Model.Location
                </span>
            </div>
            <div class="event-meta-item">
                <span class="event-meta-label">Създател</span>
                <span class="event-meta-value">
                    <i class="fas fa-user-pen me-1"></i>@Model.CreatedById
                </span>
            </div>
        </div>

        <div class="event-description">@Model.Description</div>

        <div class="event-admin-actions">
            <a asp-area="Admin" asp-controller="Events" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Редакция</a>
            <a asp-area="Admin" asp-controller="Events" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-outline-danger">Изтриване</a>
            <a class="btn btn-outline-success" target="_blank" rel="noopener noreferrer" href="https://www.google.com/maps/search/?api=1&query=@Uri.EscapeDataString(Model.Location)">
                <i class="fas fa-map-location-dot me-1"></i>Отвори карта
            </a>
            <a asp-area="Admin" asp-controller="Events" asp-action="Index" class="btn btn-outline-secondary">Назад</a>
        </div>
    </article>
</div>

